import React from 'react';
import { View, Text, Button } from 'react-native';

const RegisterBusinessScreen = ({ navigation }) => {
  return (
    <View>
      <Text>RegisterBusinessScreen</Text>
    </View>
  );
};

export default RegisterBusinessScreen;
